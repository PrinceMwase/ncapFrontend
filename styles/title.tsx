import { StyleSheet } from "react-native";

const TitleStyles = StyleSheet.create({
  
    title: {
        fontSize: 20,
        fontWeight: 'bold',
        marginTop: '10%'
      }
  });

export default TitleStyles